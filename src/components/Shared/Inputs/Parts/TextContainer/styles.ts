import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  base: {
    flexDirection: 'row',
    alignItems: 'center',
    height: 45,
    paddingLeft: 10,
    width: '100%',
    backgroundColor: '#eee',
    borderRadius: 4,
  },
});
