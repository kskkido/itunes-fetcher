import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  base: {
    width: '100%',
    height: 60,
    flexDirection: 'row',
    alignItems: 'center',
  },
  child: {
    marginBottom: 10,
    width: '100%',
  },
});
