import { StyleSheet } from 'react-native';
import containerStyles from '../Container/styles';

export default StyleSheet.create({
  base: {
    height: '100%',
    width: containerStyles.base.height,
    backgroundColor: '#ddd',
  },
});
